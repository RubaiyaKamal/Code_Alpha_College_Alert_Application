<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_splash.xml - Splash Screen Layout
    Shown for 2.5 seconds when the app launches.
    Centered content with app branding on a gradient background.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@color/colorPrimary"
    android:padding="32dp">

    <!--
        App Icon Circle - A large colored circle with a bell/notification symbol.
        We use a TextView with emoji as a simple icon placeholder.
        In production, replace with an ImageView with a proper vector drawable.
    -->
    <TextView
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:text="🔔"
        android:textSize="60sp"
        android:gravity="center"
        android:background="@drawable/circle_white_bg"
        android:layout_marginBottom="24dp" />

    <!-- App Name - Large bold title -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textSize="32sp"
        android:textStyle="bold"
        android:textColor="@android:color/white"
        android:letterSpacing="0.05"
        android:layout_marginBottom="8dp" />

    <!-- App Tagline - Subtitle text -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Stay Updated with Campus Events"
        android:textSize="14sp"
        android:textColor="#CCFFFFFF"
        android:gravity="center"
        android:layout_marginBottom="48dp" />

    <!-- Loading indicator shown at the bottom -->
    <ProgressBar
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:indeterminateTint="@android:color/white"
        android:layout_marginTop="16dp" />

    <!-- Version text at the bottom -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="v1.0"
        android:textSize="12sp"
        android:textColor="#99FFFFFF"
        android:layout_marginTop="16dp" />

</LinearLayout>
